package insert

func InsertSort(n int, arr []int) {
	// for i := 1; i < n; i++ {
	// 	preIndex := i
	// 	for j := i; j > 0; j-- {
			
	// 		if arr[j] < arr[i] {
	// 			arr[preIndex + 1] = arr[preIndex]
	// 		}
	// 		arr[]
	// 	}
	// }
}
